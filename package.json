{
    "name": "jaime-chapinal.com",
    "source": "src/index.html",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel",
        "build": "rm -rf .cache ./dist && parcel build --public-url ./",
        "deploy-ghpages": "rm -rf .cache ./dist && parcel build --public-url https://xatpy.github.io/JaimeChapinal.com_v2/ && git subtree push --prefix dist origin gh-pages"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "2.8.3",
        "@parcel/transformer-webmanifest": "2.8.3",
        "gh-pages": "^5.0.0",
        "parcel": "^2.8.3"
    }
}
