{
    "name": "jaime-chapinal.com",
    "source": "src/index.html",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel",
        "build": "rm -rf .parcel-cache ./dist && parcel build --no-source-maps --public-url ./",
        "predeploy": "rm -rf dist .parcel-cache && parcel build --no-source-maps --public-url https://xatpy.github.io/JaimeChapinal.com_v2/",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "2.8.3",
        "@parcel/transformer-webmanifest": "2.8.3",
        "gh-pages": "^5.0.0",
        "parcel": "^2.8.3"
    }
}
