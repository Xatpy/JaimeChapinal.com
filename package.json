{
    "name": "portfolio-jaimechapinal.com",
    "source": "src/index.html",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel",
        "build": "rm -rf .parcel-cache ./dist && parcel build --public-url ./",
        "predeploy": "rm -rf dist .parcel-cache && parcel build --public-url https://xatpy.github.io/JaimeChapinal.com/",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "2.8.3",
        "@parcel/transformer-webmanifest": "2.8.3",
        "gh-pages": "^5.0.0",
        "parcel": "^2.8.3"
    },
    "dependencies": {}
}
